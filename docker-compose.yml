services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scalpy
      POSTGRES_PASSWORD: scalpy
      POSTGRES_DB: scalpy
    ports:
      - "5432:5432"
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
